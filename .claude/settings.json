{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git branch --show-current 2>/dev/null); case \"$branch\" in feature/*|worktree-*) exit 0 ;; *) reason=\"Blocked: current branch is '$branch' (not feature/*). Branching policy: git checkout develop && git pull && git checkout -b feature/[JIRA]-[desc] (e.g. feature/ASAT-123-desc), then confirm with git branch --show-current before editing.\"; printf '%s' \"$reason\" | jq -Rs '{hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:.}}'; exit 0 ;; esac"
          }
        ]
      }
    ]
  }
}
